name: LANTERN corrected mapping and submission freeze v2

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repair-in-progress:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'Submission-package repair in progress. This temporary manual-only workflow is replaced by the final audited CI in the same branch.'
